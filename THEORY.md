# Orthogonal Agent Polity (OAP)

> **正交代理政体：用最低维的政体语义坐标，对 Main Agent 治理 Subagent 的关系进行拟合；具体权限只是操作化，具体行为与系统效果则是涌现结果。**

英文短句：

> **OAP fixes the polity coordinates, not their implementation.**

以及认识论原则：

> **Agents share cognition, but reality remains outside them.**
>
> **代理共享认知，但事实永远在代理之外。**

---

## 0. 理论定位

OAP 是一个用于描述多 Agent **治理关系**的低维语义框架。

它不是：

- 现实政治哲学的直接移植；
- Agent 人格分类；
- 一组永久固定的软件权限；
- “上下文开放性 × 调查自主性”的同义词。

OAP 的第一步，是从复杂的子代理治理关系中抽取两条尽量正交、足够低维、又具有解释力的**政体思潮轴**，形成一个最小 `2 × 2` 关系空间。

OAP v1 采用：

1. **威权主义 ↔ 平等主义**
2. **排外主义 ↔ 亲外主义**

这两个轴首先是**语义拟合坐标**。

它们如何映射成上下文权限、调查权、发言权、预算权、记忆共享、工具调用等工程机制，是第二层问题；这些机制最终产生什么行为，则是第三层问题。

因此 OAP 的基本结构是：

```text
复杂治理现实
    ↓ 最低正交拟合
政体语义坐标
    ↓ 操作化
Harness / Runtime 权限与编排
    ↓ 运行
Agent 行为与系统效果
```

即：

```text
Polity Semantics
      ↓
Operationalization
      ↓
Emergent Phenotype
```

**定义、实现、效果三者必须严格分离。**

---

## 1. 主权假设：Main 是统治者

OAP v1 假定存在一个拥有完整治理权的：

```text
Main Agent / Sovereign
```

Subagent 不与 Main 共享主权。

Main 保留：

- 任务分配权；
- 资源调度权；
- Agent 启停权；
- 工具授权权；
- 执行授权权；
- veto / termination；
- 政体切换权。

因此 OAP 讨论的不是：

> “Main 和 Subagent 是否拥有相同权力？”

而是：

> **Main 以什么政体治理 Subagent？**

即使某条治理边处于“平等”象限，Main 仍然是 Sovereign。

“平等主义”描述的是统治方式中的某种权力下放、主体性承认或认知地位，而不是主权对等。

---

## 2. 两条最小政体轴

### 2.1 轴 A：威权主义 ↔ 平等主义

这条轴描述：

> **统治者在多大程度上垄断议程、判断与决定权，又在多大程度上承认 Subagent 的独立主体性。**

威权主义一侧倾向于：

- 由 Main 定义任务；
- 由 Main 决定何时调查；
- 由 Main 决定哪些问题值得提出；
- Subagent 更接近被调度的工具。

平等主义一侧倾向于：

- 承认 Subagent 可以形成独立判断；
- 允许 Subagent 主动提出值得注意的问题；
- 允许其在一定范围内自行决定调查路径；
- 允许其以自己的证据对 Main 构成有效反证。

但这里**不预先规定**“平等主义一定等于调查自主权”。

调查自主权只是当前参考实现里，对这条语义轴的一种工程操作化。

其它可能的平等主义操作化还包括：

- 主动发言权；
- 独立提出任务的权利；
- 独立预算；
- 独立选择调查方法；
- 对某类操作拥有 veto；
- 对 Main 的判断拥有形式化异议通道。

哪些机制最能拟合这条轴，需要由具体 Harness 和实验决定。

---

### 2.2 轴 B：排外主义 ↔ 亲外主义

这条轴描述：

> **统治者的认知边界对外部 Agent 有多开放。**

排外主义一侧倾向于：

- Main 的内部认知不向 Subagent 开放；
- Subagent 与 Main 之间保持较强边界；
- 外部认知进入 Main 的通道受到限制；
- 不同 Agent 的判断更可能保持去相关。

亲外主义一侧倾向于：

- Main 愿意让 Subagent 接触自身当前认知；
- Main 接受 Subagent 对自身判断的补充、质疑与修正；
- Agent 之间存在更充分的认知交换；
- 对方的认知可以成为寻找新证据的线索。

但这里同样**不预先规定**“亲外主义一定等于注入 Main 最新上下文”。

上下文开放性只是当前参考实现中的一种操作化。

其它可能的亲外主义操作化包括：

- 开放长期记忆；
- 开放当前假设；
- 开放不确定性与疑点；
- 允许未经邀请的反馈；
- 接受跨 Agent 的证据引用；
- 允许外部 Agent 读取某些决策依据；
- 允许外部 Agent 持续订阅认知变化。

因此：

```text
亲外主义 ≠ Context Openness 本身
```

而是：

```text
亲外主义
    ↓ 某次工程操作化
Context Openness
```

---

## 3. 四象限：政体语义空间

两条二极轴构成最小 `2 × 2` 政体空间：

```text
                    亲外
                     ↑
                     │
          威权亲外   │   平等亲外
                     │
威权 ────────────────┼──────────────── 平等
                     │
          威权排外   │   平等排外
                     │
                     ↓
                    排外
```

四象限分别是：

1. **威权排外**
2. **威权亲外**
3. **平等排外**
4. **平等亲外**

这四个名称首先表示**治理语义位置**。

它们并不永久等价于：

```text
被动工具 / 知情参谋 / 独立调查者 / 开放事实伙伴
```

后面四个角色只是当前 DeepSeek + Seed 参考实现中，特定操作化后涌现出的第一组典型表型。

---

## 4. 三层模型：语义、操作化、表型

### 4.1 第一层：Polity Semantics / 政体语义

这一层只有：

```text
威权 ↔ 平等
排外 ↔ 亲外
```

它负责提供最低维的治理坐标系。

这一层不直接规定具体 API、Prompt、Tool、Context、Memory、Budget 或 Permission。

---

### 4.2 第二层：Operationalization / 操作化

操作化回答：

> “在当前 Agent Harness 中，如何把某个政体语义位置变成可运行机制？”

设操作化函数为：

```text
O = Ψ(P, Harness, Environment)
```

其中：

- `P` = 政体语义位置；
- `Harness` = 当前 Agent 运行平台与能力；
- `Environment` = 当前场景、风险、预算、时限等；
- `O` = 实际运行时机制。

操作化可以涉及：

- 上下文是否共享；
- 共享什么上下文；
- 是否允许主动调查；
- 搜索预算；
- 是否允许主动发言；
- 是否允许提出新任务；
- Memory 是否共享；
- 是否允许独立选择工具；
- 是否设置 veto；
- 是否允许并行调查；
- Main 是否必须阅读 Subagent 报告。

**OAP 固定的是坐标，不固定这些工程实现。**

---

### 4.3 第三层：Emergent Phenotype / 涌现表型

运行以后才观察：

- 是否主动纠错；
- 是否产生 Blind Review；
- 是否降低认知相关性；
- 是否发生上下文污染；
- 是否形成群体共识；
- 是否增强自我强化；
- 是否逃离错误认知吸引子；
- 是否增加成本和延迟；
- 是否出现新的失败模式。

设：

```text
Φ = F(P, O, Models, Task, Environment, Randomness)
```

其中 `Φ` 是实际观察到的系统表型。

因此：

```text
01 平等排外 ≠ Blind Review
```

而是：

```text
01 平等排外
+ 某种操作化
+ 某种任务环境
        ↓
可能涌现 Blind Review / 去相关性
```

同理：

```text
11 平等亲外 ≠ 主动纠错
```

而只是某些操作化下可能涌现：

```text
理解 Main
→ 主动找证据
→ 反馈反证
→ Main 修正
```

这些都是**实验发现的效果，不是理论定义**。

---

## 5. 当前参考操作化 v0.1

为了让 OAP 可以直接进入代码和实验，当前 DeepSeek + Seed 原型采用一套最小映射。

定义：

```text
C = Context Openness
I = Investigative Autonomy
```

其中：

```text
C ∈ {0,1}
I ∈ {0,1}
```

当前映射 Ψ₀ 为：

```text
排外主义  → C = 0
亲外主义  → C = 1

威权主义  → I = 0
平等主义  → I = 1
```

于是得到当前参考实现：

| 政体语义 | C | I | 当前工程实现 |
|---|---:|---:|---|
| 威权排外 | 0 | 0 | 不共享 Main 最新认知；不允许 Subagent 主动扩大调查 |
| 威权亲外 | 1 | 0 | 共享 Main 最新认知；调查议程仍由 Main 决定 |
| 平等排外 | 0 | 1 | 不共享 Main 当前判断；允许 Subagent 独立调查 |
| 平等亲外 | 1 | 1 | 共享 Main 当前判断；允许 Subagent 主动调查 |

**注意：这张表描述的是第一套实验映射，不是 OAP 的永恒定义。**

因此应写：

```text
威权 / 平等  --Ψ₀--> Investigative Autonomy
排外 / 亲外  --Ψ₀--> Context Openness
```

而不是写成：

```text
平等 = Investigative Autonomy
亲外 = Context Openness
```

---

## 6. 当前参考表型 v0.1

在 Ψ₀ 操作化下，我们预计可能出现以下表型：

### 6.1 威权排外 → “被动工具”表型

可能表现为：

- Subagent 不知道 Main 当前相信什么；
- 不主动扩大调查；
- 被要求做什么才做什么；
- 成本与延迟最低；
- 行为最可控。

这是一个**预测表型**，不是威权排外的定义。

### 6.2 威权亲外 → “知情参谋”表型

可能表现为：

- Subagent 知道 Main 当前判断；
- 能指出已有信息中的矛盾；
- 但不会自行开启大规模调查。

### 6.3 平等排外 → “独立调查者”表型

可能表现为：

- Subagent 不知道 Main 当前推理；
- 但可以独立接触现实；
- 可能形成与 Main 去相关的证据路径；
- 可能适合 Blind Review、独立审计与安全检查。

这不是“平等排外天然等于 Blind Review”，而是 Ψ₀ 下的一种潜在涌现。

### 6.4 平等亲外 → “开放事实伙伴”表型

可能表现为：

- Subagent 知道 Main 当前认知；
- 可以针对性寻找补证与反证；
- 可以持续把新证据推回 Main；
- 也可能因为共享 Main 叙事而提高错误相关性。

因此 11 既可能产生更强纠错，也可能产生更强认知污染。

哪一种占优必须由实验决定。

---

## 7. 认识论原则：Agent 不是事实本身

> **Agent 可以成为彼此的信息来源，但不能成为彼此的事实来源。**

Main 与 Subagent 都可能错。

因此：

```text
Main 的判断 ≠ 事实
Subagent 的判断 ≠ 事实
Agent 间共识 ≠ 事实
多数票 ≠ 事实
```

OAP 中的“找茬”也不等于：

> B 必须反对 A。

而是：

> **系统始终保留由外部事实推翻任何 Agent 的能力。**

合法反馈包括：

- 支持；
- 补充；
- 限定条件；
- 发现遗漏；
- 提出反例；
- 提出替代解释；
- 表示证据不足；
- 承认 Main 正确；
- 承认 Subagent 自己错误。

---

## 8. 调查、知情、行动必须分层

OAP 的政体坐标可以通过很多不同权限被操作化，但这些权限本身不能重新混成一个“自主性”总变量。

尤其要区分：

```text
知情权
调查权
发言权
行动权
```

当前参考实现只重点使用前两类：

```text
Context Openness
Investigative Autonomy
```

它们不自动推出：

```text
点击按钮
修改文件
提交表单
删除内容
部署代码
```

因此：

```text
调查自主权 ≠ 行动自主权
```

执行权限应属于独立的 Capability / Permission System。

同样，未来即使把“发言权”或“预算权”作为平等主义的新操作化，也不代表这些权限天然属于 OAP 的理论轴。

---

## 9. 政体属于治理边，不属于 Agent 人格

不要说：

```text
“Vision Agent 是平等亲外型 Agent。”
```

而应说：

```text
Main --[平等亲外]--> Vision
Main --[平等排外]--> Security
Main --[威权排外]--> Git
```

因此 OAP 可以表示为有根治理图：

```text
G = (V, E, M)
```

其中：

- `V` = Agents；
- `M` = Main / Sovereign；
- `E` = Main 对各 Subagent 的治理边。

每条边首先具有一个政体语义标签：

```text
p(M → S_i) ∈ {
    威权排外,
    威权亲外,
    平等排外,
    平等亲外
}
```

随后再由当前 Harness 把它操作化成具体 Runtime 配置。

所以：

```text
Polity Edge ≠ Permission Vector
```

Permission Vector 只是 Polity Edge 的某次实现。

---

## 10. 动态政体

OAP 不假设某一种政体永远最佳。

政体可以是运行时状态：

```text
p_t(M,S) → p_(t+1)(M,S)
```

Main 可以根据：

- 时间压力；
- 资源预算；
- 失败成本；
- 操作不可逆性；
- 环境敌意；
- 信息不确定性；

改变对子代理的治理方式。

但“如何改变权限”仍属于操作化层。

例如同一个：

```text
平等亲外 → 威权排外
```

在不同 Harness 里可以表现为不同 Runtime 动作：

- 关闭上下文共享；
- 关闭主动工具调用；
- 禁止主动发言；
- 收回独立预算；
- 只保留 veto；
- 降低消息频率。

OAP 只规定政体语义发生了收缩，不预先指定唯一实现。

---

## 11. Garden / Abyss 是环境原型，不是理论轴

### Garden / 田园

典型：

- 时间较充足；
- 资源可接受；
- 错误可恢复；
- 可以反复验证；
- 环境敌意较低。

这里较开放、较平等的政体可能具有更高认识论收益。

### Abyss / 深渊

典型：

- 时间极短；
- 资源极少；
- 操作不可逆；
- 失败成本巨大；
- 沟通和调查本身具有成本。

这里较威权、较排外的治理可能成为局部生存最优。

但这些都是：

```text
环境 → 政体选择
```

而不是 OAP 新增第三轴。

---

## 12. 可逆的认知拓扑

成熟系统的目标不是永远开放，也不是永远集中。

而是：

> **可以随着环境变化，收缩和重新展开治理关系。**

例如：

```text
平等亲外
    ↓ 资源不足 / 风险升高
威权亲外
    ↓ 危机
威权排外
```

危机解除后：

```text
威权排外
    ↓
恢复部分自由
    ↓
威权亲外 / 平等排外
    ↓
重新展开
    ↓
平等亲外
```

这里真正变化的是：

```text
Polity Semantic State
```

具体开关变化由当前操作化函数决定。

---

## 13. 多模态 Agent：第一典型实验域

OAP 的第一真实参考系统采用：

```text
DeepSeek V4 Main / Sovereign
        ↓ OAP 治理
Seed Multimodal Subagent
```

Main 尽量使用高能力文本推理模型；Subagent 必须能够直接接触 Main 无法直接看到的多模态事实。

当前 Ψ₀ 操作化中：

```text
亲外 / 排外
→ 是否向 Seed 暴露 Main 当前认知

平等 / 威权
→ 是否允许 Seed 主动扩大事实调查
```

然后观察是否涌现：

- 主动纠错；
- Blind Review；
- 认知去相关；
- 上下文污染；
- 错误吸引子逃逸；
- 误纠偏；
- 成本变化。

实验验证的是：

> **这套操作化是否是一种有价值的 OAP 拟合。**

而不是：

> “Context Openness 和 Investigative Autonomy 就是 OAP 的永恒本体。”

---

## 14. 吸引子实验的理论地位

当前实验关注：

> Main 已经形成一个错误认知吸引子以后，不同政体操作化是否改变其逃逸概率。

形式上：

```text
Main → H0
      ↓
Subagent / 外部事实
      ↓
Main → H1
```

观察：

```text
Correction Rate
Resistance Rate
False Correction Rate
Uncertainty Preservation
Cost / Latency
```

这些指标都是**表型层指标**。

如果 01 比 11 更容易逃离某类错误吸引子，我们可以提出：

> 当前“平等排外 → Blind Independent Investigation”的操作化具有去相关价值。

但不能反过来把这种结果写进“平等排外”的定义。

理论必须允许未来实验推翻当前操作化。

---

## 15. 可证伪性与理论更新

OAP v1 应主动允许以下失败：

### 15.1 轴不正交

如果大量实验表明：

```text
威权 / 平等
```

和：

```text
排外 / 亲外
```

在 Agent 治理中无法稳定独立变化，那么当前二维拟合需要修订。

### 15.2 操作化不良

如果：

```text
平等主义 → Investigative Autonomy
```

不能稳定产生可解释行为差异，则应更换操作化，而不是强行保护现有代码。

### 15.3 表型不成立

如果所谓 Blind Review、去相关、主动纠错或吸引子逃逸无法稳定复现，则这些只能被降级为偶发现象。

### 15.4 环境依赖

如果同一政体在不同任务、模型、预算和风险环境下产生相反效果，应把差异保留在环境与操作化层，而不是把某种政体宣布为永久最优。

---

## 16. OAP v1 暂时不做什么

为了保持最低拟合，初版不把以下内容并入核心轴：

- Trust score；
- Reputation；
- Agent 等级；
- Voting；
- Belief graph；
- 连续十几个参数；
- 行动权限；
- 资源预算；
- 记忆系统；
- 具体模型人格。

这些可以成为：

```text
操作化变量
环境变量
权限系统
观测指标
```

但不应因为工程上有用，就自动升级成 OAP 的理论原语。

---

## 17. 最小形式化

设：

```text
G = (V, E, M)
```

其中：

- `M` = Sovereign Main Agent；
- `V` = Agents；
- `E` = 治理边。

对每条 Main → Subagent 治理边：

```text
p_t(M,S) = (A_t, X_t)
```

其中：

```text
A_t ∈ {威权, 平等}
X_t ∈ {排外, 亲外}
```

于是：

```text
p_t ∈ {
  威权排外,
  威权亲外,
  平等排外,
  平等亲外
}
```

运行时实现：

```text
O_t = Ψ(p_t, Harness, Environment)
```

观察到的表型：

```text
Φ_t = F(p_t, O_t, Models, Task, Environment, Randomness)
```

动态切换：

```text
p_(t+1) = Π(p_t, Environment, Risk, Budget, Uncertainty)
```

其中：

- `Ψ` = 操作化函数；
- `F` = 实际系统动力学；
- `Π` = Main 的政体选择策略。

OAP 研究的核心不是假定 `Ψ` 和 `F` 已知，而是：

> **用一个足够小的政体语义空间，组织、比较和实验这些不同的治理实现与涌现效果。**

---

## 18. 一句话版本

英文：

> **Orthogonal Agent Polity models subagent governance through a minimal orthogonal polity space; runtime permissions are operationalizations, and agent behaviors are emergent phenotypes rather than definitions of the polity itself.**

中文：

> **正交代理政体用最小正交政体空间拟合对子代理的治理；运行时权限只是操作化，Agent 行为与系统效果则是涌现表型，而不是政体本身的定义。**

更短的核心：

> **固定坐标，不固定实现；观察涌现，不把涌现倒写成定义。**
